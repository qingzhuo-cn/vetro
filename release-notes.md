Vetro v2.3.0 — 拖拽增强 + 更新检测

- **图片拖入插入**：把图片文件从资源管理器直接拖进编辑器即可插入（也支持 Ctrl+V 粘贴）
- **文档树拖拽**：拖动文档到另一文档上成为其子文档，拖到空白处提升为主文档（防止循环嵌套）
- **侧栏优化**：收起后点标签展开更顺滑、不再回弹，展开后保持
- **检查更新**：启动后自动检测新版本；「⚙ 设置 → 检查更新」可手动检测并显示下载地址

技术栈：Tauri 2 + React 19 + TypeScript + Vite + CodeMirror 6 + Zustand + SQLite(FTS5)

功能：
- 文档树（主/子文档多级嵌套，支持拖拽调整层级）
- Markdown 实时预览（marked + highlight.js + DOMPurify）
- 8 种强调色 + 4 种图标 + 字号调节（浅色/深色/自动）
- AI 助手：获取模型 + 流式对话，API 密钥存入系统钥匙串（DPAPI/Keychain）
- SQLite 本地存储 + FTS5 全文搜索（顶栏 🔍）
- WebDAV 同步（上传 / 下载）
- 查找替换（Ctrl+F）、大纲导航、图片拖入/粘贴
- 回收站、导出（合并子文档为主文档）
- 无边框窗口 + 自定义标题栏
- 内置插件系统
