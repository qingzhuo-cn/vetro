Vetro v2.4.0 — 所见即所得（WYSIWYG）预览

- **预览可直接编辑**：在预览里改文字/标题，自动同步回 Markdown 源码（CodeMirror 同步更新）
- **外部图片拖入预览**：从文件管理器把图片直接拖进预览即可插入（也支持拖进编辑器 / Ctrl+V）
- **预览内拖动图片调整位置**：直接拖动预览中的图片到新位置，源码中的图片位置同步改变
- 支持把图片从预览拖出/换位，双向同步保持一致

技术栈：Tauri 2 + React 19 + TypeScript + Vite + CodeMirror 6 + Zustand + SQLite(FTS5)

功能：
- 文档树（主/子文档多级嵌套，支持拖拽调整层级）
- **Markdown 所见即所得预览**（可编辑、可拖图片）+ 实时预览
- 8 种强调色 + 4 种图标 + 字号调节（浅色/深色/自动）
- AI 助手：获取模型 + 流式对话，API 密钥存入系统钥匙串（DPAPI/Keychain）
- SQLite 本地存储 + FTS5 全文搜索（顶栏 🔍）
- WebDAV 同步（上传 / 下载）
- 查找替换（Ctrl+F）、大纲导航、图片拖入/粘贴
- 回收站、导出（合并子文档为主文档）
- 无边框窗口 + 自定义标题栏
- 检查更新（自动 + 手动）
- 内置插件系统
