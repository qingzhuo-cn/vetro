Vetro v2.4.3 — 代码审查修复

本次为稳定性修复，无新增功能：

- 修复 WYSIWYG 双向同步竞态：预览编辑 → 编辑器同步回写；切换编辑区域不再丢内容
- 文档树 / 导出增加环保护（脏数据不会导致栈溢出）
- 修正浏览器态版本号、JSON 解析兜底（AI 接口返回非 JSON 时给出友好提示）
- 编辑器粘贴/拖入图片时文件名转义（含 `]`/`(` 等不再破坏 Markdown）
- AI 流式兼容 reasoner 模型的思考内容输出
- 清理未使用导入

（历史版本均保留，可随时回退）
